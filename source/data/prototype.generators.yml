type: string
article: number
manufacturer:
  brand: string
  country: string
series: string|number|false
model: string|number|false
# patttern for outputing name of the paricular model with `sprintf()`
title: string("%(brand)s %(series)s %(model)s")|string
price: number
discount: number
availability: available|preorder|discontinued
warranty:
  delivery: number
  installation: number|false
  # time spent in active use (e.g. working hours, motoresource)
  serviceLife: number|false
desc: string|array|false
features: array(title, desc)|false
photos: array(url, alt, main)|false
videos: array(youtubeIframeSrc)|false
docs: array(url, name, desc)|false
tags: array(tag)|false

-----------------------------------------

specs:
  # мощность
  power:
    ng:
      max:
    lpg:
      max:
  # сила тока
  current:
    ng:
    lpg:
  # напряжение (необходимое напряжение в сети)
  voltage: number
  # частота
  frequency: number
  # количество фаз
  phases: number
  # коэфициент мощности
  phi:
  # защитный автомат, А
  circuitBreaker: number
  fuel:
    ng: 0.4 м3/ч * 1 кВт
    lpg: 0.350 кг/ч * 1 кВт
  operatingSpeed:
  cooling:

-----------------------------------------

  starter: Электростартер|Ручной
  nonstopOperatingTime: Круглосуточно


  electronics:
    controlBlock: LCD-дисплей
    controlBlockLanguage: Русский
    automaticTransferSwitch: true
    automaticTransferSwitchTime: 30 сек
    autocontrolOfGeneratorsWork: true
    cascade: До 10 генераторов

-----------------------------------------

equipment: array|false

engine:
  type:
  model:
  manufacturer:
    brand:
    country:
  # 8.2kW@3600rpm
  power:
    hp: number
    value: number
    rpm: number
  # 25.1N.m@2500rpm
  torque:
    value:
    rpm:
  # расположение клапанов
  valvetrain: OHV|SOHC|DOHC
  # количество цилиндров
  cylinders:
  # количество тактов
  strokes:
  # объем двигателя, куб.см
  displacement:
  compressionRatio:
  operatingSpeed:
  cooling: air|liquid
  oilCapacity:
  fuel:
    ng: 0.4 м3/ч * 1 кВт
    lpg: 0.350 кг/ч * 1 кВт

-----------------------------------------

alternator:
  type:
  model:
  manufacturer:
    brand:
    country:
  insulation:
  protection: IP64|IP52|...|false

-----------------------------------------

enclosure:
  # тип
  type: корпус|открытый|контейнер... (current:: open|soundproof)
  # материал
  material: сталь|нержавеющая сталь|...|false
  # покрытие
  coating: string|false
  # теплоизоляция
  thermalInsulation: bool
  # маркировка защиты (https://en.wikipedia.org/wiki/IP_Code)
  protection: IP64|IP52|...|false
  # вентиляция
  ventilation: bool
  # замок
  lock: string(type)|bool
  # съемные стенки
  removablePanels: bool
  # уровень шума
  noise: number
  # высота всего изделия
  height: number
  # ширина всего изделия
  width: number
  # глубина всего изделия
  depth: number
  # вес всего изделия
  weight: number

-----------------------------------------

controls:
  type:
  model:
  manufacturer:
    brand:
    country:

