---
generatorId:     фас-55-ozp3-m-55-квт

title:           "{{ selectGenerator(GENERATORS, 'фас-55-ozp3-m-55-квт').meta.title }}"
breadcrumbTitle: "{{ selectGenerator(GENERATORS, 'фас-55-ozp3-m-55-квт').title }}"
contentTitle:    "{{ selectGenerator(GENERATORS, 'фас-55-ozp3-m-55-квт').title }}"
navTitle:        "{{ selectGenerator(GENERATORS, 'фас-55-ozp3-m-55-квт').title }}"
metaDesc:        "{{ selectGenerator(GENERATORS, 'фас-55-ozp3-m-55-квт').meta.desc }}"
---

{% extends './_generator.nj' %}