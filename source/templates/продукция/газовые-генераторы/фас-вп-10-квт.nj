---
generatorId:     фас-вп-10-квт

title:           "{{ selectGenerator(GENERATORS, 'фас-вп-10-квт').meta.title }}"
breadcrumbTitle: "{{ selectGenerator(GENERATORS, 'фас-вп-10-квт').title }}"
contentTitle:    "{{ selectGenerator(GENERATORS, 'фас-вп-10-квт').title }}"
navTitle:        "{{ selectGenerator(GENERATORS, 'фас-вп-10-квт').title }}"
metaDesc:        "{{ selectGenerator(GENERATORS, 'фас-вп-10-квт').meta.desc }}"
---

{% extends './_generator.nj' %}