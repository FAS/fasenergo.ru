---
generatorId:     фас-55-ozp3-m-55-квт

title:           "{{ selectGenerator(data, 'фас-55-ozp3-m-55-квт').meta.title }}"
breadcrumbTitle: "{{ selectGenerator(data, 'фас-55-ozp3-m-55-квт').title }}"
contentTitle:    "{{ selectGenerator(data, 'фас-55-ozp3-m-55-квт').title }}"
navTitle:        "{{ selectGenerator(data, 'фас-55-ozp3-m-55-квт').title }}"
metaDesc:        "{{ selectGenerator(data, 'фас-55-ozp3-m-55-квт').meta.desc }}"
---

{% extends '../_generator.nj' %}