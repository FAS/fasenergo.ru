---
generatorId:     фас-вп-10-квт

title:           "{{ selectGenerator(data, 'фас-вп-10-квт').meta.title }}"
breadcrumbTitle: "{{ selectGenerator(data, 'фас-вп-10-квт').title }}"
contentTitle:    "{{ selectGenerator(data, 'фас-вп-10-квт').title }}"
navTitle:        "{{ selectGenerator(data, 'фас-вп-10-квт').title }}"
metaDesc:        "{{ selectGenerator(data, 'фас-вп-10-квт').meta.desc }}"
---

{% extends '../_generator.nj' %}