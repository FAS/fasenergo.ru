---
title:           Адсорбирующее оборудование
contentTitle:    Адсорбирующее оборудование
navTitle:        Адсорбирующее оборудование
breadcrumbTitle: Адсорбирующее оборудование

description: Адсорбирующее оборудование
---

{% extends '_layouts/_main.nj' %}

{% block main %}
{# @todo Нет контента #}
<ul>
  {% for slug, item in getPage(PAGE.breadcrumb) %}
  <li><a href='{{ item.props.url }}'>{{ item.props.navTitle }}</a></li>
  {% endfor %}
</ul>
{% endblock %}